
/**
 * get HTML elemnts we will use 
 * document.querySelectorAll = finds ALL matching elements
  document.getElementById = finds ONE element by ID
  const = stores the element for later use
 */


// get filter button and add event listener to it
const filterbutton = document.getElementById('.filter-btn')
const searchInput = document.getElementById('searchInput')
const sortDropdown = document.getElementById('sortSelect')

// get trail cards container
const card = document.querySelectorAll('.card')


// get favorite buttons and add event listeners to them
const favCount = document.getElementById('favCount')

// get all favorite buttons

const favoriteButtons = document.getselctorAll('.favorite-btn')




